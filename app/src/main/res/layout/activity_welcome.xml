<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EEF3FF">

    <!-- Full-screen welcome image (loaded async in Activity) -->
    <ImageView
        android:id="@+id/imgWelcome"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="@null"/>

    <!-- Light scrim at bottom so buttons sit cleanly -->
    <View
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_scrim_bottom_light"/>

    <!-- Buttons pinned to bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="36dp">

        <!-- Primary: Bắt đầu ngay → Register -->
        <Button
            android:id="@+id/btnGetStarted"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:text="Bắt đầu ngay"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/bg_btn_welcome_primary"
            android:stateListAnimator="@null"/>

        <!-- Secondary: Đã có tài khoản → Login -->
        <Button
            android:id="@+id/btnHaveAccount"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginTop="12dp"
            android:text="Đã có tài khoản"
            android:textColor="#2875FB"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/bg_btn_welcome_secondary"
            android:stateListAnimator="@null"/>

    </LinearLayout>

</FrameLayout>
